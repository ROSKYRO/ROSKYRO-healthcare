import ReferralsList from '../shared/ReferralsList';

export default function AllReferrals() {
  return (
    <ReferralsList
      title="All Referrals"
      subtitle="Full oversight across every healthcare business and partner in the network."
      basePath="/team/referrals"
    />
  );
}
